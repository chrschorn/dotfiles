- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.shell:
    ~/.bash:
    ~/.vim: vim-config
    ~/.vimrc: vim-config/vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [vim +PlugUpdate +qall, Installing vim plugins]
